<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:gamedata="clr-namespace:PokeDB.GameData;assembly=PokeDB"
             xmlns:views="clr-namespace:PokeDB.Views;assembly=PokeDB"
             x:Class="PokeDB.PokemonSearch.PokemonSearchPage">

  <ContentPage.Resources>
    <ResourceDictionary>
      <gamedata:PokemonIdToImageUrlConverter x:Key="PokemonToImageUrlConverter"/>
    </ResourceDictionary>
  </ContentPage.Resources>
  
  <ListView x:Name="PokemonList" ItemsSource="{Binding Pokemon}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout Orientation="Horizontal" Padding="6">
            <views:RemoteImage ImageUrl="{Binding Id, Converter={StaticResource PokemonToImageUrlConverter}}" WidthRequest="36" HeightRequest="36" />
            <Label Text="{Binding Name}"
                   HorizontalOptions="FillAndExpand"
                   VerticalOptions="Start"
                   FontSize="16"/>
          </StackLayout>
        </ViewCell>        
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
  
</ContentPage>